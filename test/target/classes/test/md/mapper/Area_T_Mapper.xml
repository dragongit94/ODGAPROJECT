<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test.md.mapper.Area_T_Mapper">
	<select id="list" resultType="Area_T">
	    select * from AREA_T order by rownum
	</select>
	<select id="list_s" resultType="Sigungu_T">
	    select * from SIGUNGU_T order by rownum
	</select>

</mapper>